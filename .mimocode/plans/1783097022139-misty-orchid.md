# Plan: Remove "Normally" price display

## Goal
Remove the "Normally $X" price that shows when yearly billing is selected, since "2 months free" already communicates the discount.

## Change: `src/components/pricing/PricingSection.tsx`

Remove lines 201-205:
```tsx
{isYearly && (
  <p className="mt-1 text-xs text-gray-400">
    ({locale === 'fa' ? 'معمولاً' : locale === 'tr' ? 'Normalde' : 'Normally'} {formatPrice(locale, plan.price as number)})
  </p>
)}
```

## Verification
- `npm run build` passes
