# Plan: Commit animation changes

## Summary
Commit all page transition and scroll animation changes made in this session.

## Changes to commit
1. **Page transitions**: Added `framer-motion` + `template.tsx` for route-change fade/slide transitions
2. **Reusable animation components** (`src/components/animations/`):
   - `FadeIn.tsx` — scroll-triggered entrance with configurable direction
   - `StaggerChildren.tsx` + `StaggerItem` — staggered children animations
   - `AnimatedCounter.tsx` — count-up number animation
3. **Home page components updated** with scroll animations:
   - `HomeHero.tsx` — staggered fade-in for kicker, title, description, CTAs, image
   - `HomeStats.tsx` — AnimatedCounter for stat numbers
   - `HomeHighlights.tsx` — StaggerChildren for bento cards
   - `HomeHowItWorks.tsx` — StaggerChildren for step circles
   - `HomeTrust.tsx` — StaggerChildren for trust cards
4. **Features page** — StaggerChildren for 10 feature cards
5. **PricingSection** — StaggerChildren for pricing cards + fade-in for comparison table
6. **Home page** (`page.tsx`) — removed unused `locale` prop from HomeStats

## Verification
- `npx next build` — already passed ✓
- `npx eslint` — 1 pre-existing warning (`comparisonFeatures` unused), no new issues ✓
